<% @songs.each do |song| %>
<p>Song Name: <%= link_to song.name, song_path(song) %> </p><br>
<p>Artist: <%= link_to song.artist.name, artist_path(song.artist.id)%> </p><br>
<p>Genre: <%= link_to song.genre.name, genre_path(song.genre.id) %> </p>
<% end %>
